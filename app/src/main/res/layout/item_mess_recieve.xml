<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:fitsSystemWindows="true">
    <data>

    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:background="@color/colorWhite"
        android:padding="@dimen/margin_10dp_"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tv_message_recieve"
            android:layout_width="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:padding="@dimen/margin_10dp_"
            android:maxWidth="200dp"
            android:layout_marginLeft="@dimen/margin_10dp_"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            android:background="@drawable/bg_mess_recievee"
            android:text="Heloo chào bạn nha .Mình làm quen nhé :3"
            android:textColor="@color/colorBlack"
            android:textSize="@dimen/txt_size_content"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>